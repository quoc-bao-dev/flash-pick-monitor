import { ExtensionsView } from '@/modules/extensions/views/ExtensionsView';

export const metadata = {
  title: 'Extensions - Flash Pick Monitor',
};

export default function ExtensionsPage() {
  return <ExtensionsView />;
}
